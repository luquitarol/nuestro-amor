/* ===================================================== 💛 FECHA DE VUESTRA RELACIÓN ===================================================== CAMBIA ESTOS NÚMEROS. Ejemplo: 21 de septiembre de 2024 a las 18:30 new Date(2024, 8, 21, 18, 30) IMPORTANTE: Los meses empiezan en 0. Enero = 0 Febrero = 1 Marzo = 2 Abril = 3 Mayo = 4 Junio = 5 Julio = 6 Agosto = 7 Septiembre = 8 Octubre = 9 Noviembre = 10 Diciembre = 11 ===================================================== */ const fechaInicio = new Date( 2025, 6, 12, 20, 30 ); /* ===================================================== ABRIR LA PÁGINA ===================================================== */ const botonFlor = document.getElementById("botonFlor"); const pantallaInicial = document.getElementById("pantallaInicial"); const contenido = document.getElementById("contenido"); botonFlor.addEventListener( "click", function () { pantallaInicial.classList.add("oculta"); setTimeout( function () { contenido.classList.add("visible"); }, 500 ); } ); /* ===================================================== CORAZONES FLOTANTES ===================================================== */ const arbol = document.getElementById( "corazonGirasoles" ); const contenedorCorazones = document.getElementById( "corazonesFlotantes" ); arbol.addEventListener( "click", crearCorazones ); function crearCorazones() { /* Cada clic genera 18 corazones. */ for ( let i = 0; i < 18; i++ ) { const corazon = document.createElement( "span" ); /* Diferentes tipos de corazones. */ const tipos = [ "💛", "💛", "💛", "💛", "♡", "♥" ]; corazon.textContent = tipos[ Math.floor( Math.random() * tipos.length ) ]; corazon.classList.add( "corazon-flotante" ); /* Posición horizontal aleatoria. */ const posicionX = 50 + ( Math.random() * 30 - 15 ); corazon.style.left = posicionX + "%"; /* Tamaño aleatorio. */ const tamaño = 15 + Math.random() * 25; corazon.style.fontSize = tamaño + "px"; /* Duración aleatoria. */ const duracion = 3 + Math.random() * 3; corazon.style.animationDuration = duracion + "s"; /* Retraso aleatorio. */ const retraso = Math.random() * 0.8; corazon.style.animationDelay = retraso + "s"; /* Movimiento lateral. */ const desplazamiento = ( Math.random() * 240 ) - 120; corazon.style.setProperty( "--x", desplazamiento + "px" ); /* Rotación aleatoria. */ const rotacion = ( Math.random() * 60 ) - 30; corazon.style.setProperty( "--rotacion", rotacion + "deg" ); /* Añadimos el corazón a la página. */ contenedorCorazones.appendChild( corazon ); /* Lo eliminamos cuando termina la animación. */ setTimeout( function () { corazon.remove(); }, ( duracion + retraso ) * 1000 + 500 ); } } /* ===================================================== CONTADOR ===================================================== */ function actualizarContador() { const ahora = new Date(); let diferencia = ahora - fechaInicio; if (diferencia < 0) { diferencia = 0; } const segundo = 1000; const minuto = segundo * 60; const hora = minuto * 60; const dia = hora * 24; const dias = Math.floor( diferencia / dia ); const horas = Math.floor( (diferencia % dia) / hora ); const minutos = Math.floor( (diferencia % hora) / minuto ); const segundos = Math.floor( (diferencia % minuto) / segundo ); document.getElementById( "dias" ).textContent = dias; document.getElementById( "horas" ).textContent = String(horas).padStart( 2, "0" ); document.getElementById( "minutos" ).textContent = String(minutos).padStart( 2, "0" ); document.getElementById( "segundos" ).textContent = String(segundos).padStart( 2, "0" ); const fechaFormateada = fechaInicio.toLocaleDateString( "es-ES", { day: "numeric", month: "long", year: "numeric" } ); document.getElementById( "fechaTexto" ).textContent = "Desde el " + fechaFormateada + " 💛"; } actualizarContador(); setInterval( actualizarContador, 1000 );
